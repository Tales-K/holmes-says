make this a git repo

create a page using github static pages

make it mordern and dark theme fancy using new glass style from apple

add libs if needed, use npm or yarn

create a page that allows the user to input a mp3 or ogg file

the user can fill a openai api key

then we will use openai whisper to transcribe the audio file

the transcription will be displayed on the page in a nice format

the user can download the transcription as a text file

save it in local storage so if the user comes back they can see their previous transcriptions

add a link to openai to create an api key

add a button to summaryze provided text

add language button with portuguese or english, default ptbr


